"""AMEL inference API (Milestone 5): serves `models:/higgs_decision_tree@champion`
over raw features or Feast online features, persists every prediction,
publishes it to `predictions.v1`, and never lies about its own health."""
