# AMEL environment configuration — copy to .env and fill in real values.
# Never commit .env. This file documents required variables with
# placeholder values only.

# --- General ---
ENVIRONMENT=development
LOG_LEVEL=info

# Milestone-specific variables (Postgres, Kafka, MinIO, Feast/Redis,
# MLflow, JWT signing keys, LLM provider keys, etc.) are appended here as
# each subsystem is implemented — see PROJECT_STATE.md for what exists
# today. Do not pre-declare variables for services that don't exist yet.
